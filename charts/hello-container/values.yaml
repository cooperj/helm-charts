replicaCount: 1

image:
  repository: ghcr.io/cooperj/hello-container
  pullPolicy: IfNotPresent

imagePullSecrets: []

# All container environment variables are defined here as a simple map,
# so any of them can be overridden at install time, e.g.:
#   helm install my-app . --set env.MESSAGE="Hello!"
# PORT doubles as the container's listening port (used for containerPort
# and as the default service targetPort below).
env:
  NAME: "My Cluster"
  MESSAGE: "Overide the values for this helm chart to configure what is outputted here!"
  PORT: "3000"
  EMOJI: "🤖"

service:
  type: ClusterIP
  port: 3000

resources: {}
# resources:
#   limits:
#     cpu: 100m
#     memory: 128Mi
#   requests:
#     cpu: 50m
#     memory: 64Mi

nodeSelector: {}
tolerations: []
affinity: {}

podAnnotations: {}
podSecurityContext: {}
securityContext: {}

serviceAccount:
  create: false
  annotations: {}
  name: ""
